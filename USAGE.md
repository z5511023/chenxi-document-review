# 辰溪工程文件审核助手 - 使用说明文档

## 一、产品简介

**辰溪工程文件审核助手**是辰溪抽水蓄能电站数字化管控平台的子模块，基于 AI 大模型 + 模块化知识库 + 联网搜索，实现工程文件的智能审核、错别字检测和对比标注。

### 核心能力

- **模块化知识库审核**：6 种审核类型分别对应独立的知识库，精准匹配审核标准
- **错别字自动检测**：AI 逐字检查并用红色标注错别字和用词错误
- **左右对比标注**：左侧原始文件，右侧 AI 标注结果，问题一目了然
- **联网搜索补全**：知识库不足时自动联网搜索最新法规标准
- **账号权限管理**：管理员/普通用户/游客三级权限，数据隔离

---

## 二、系统架构

```
┌─────────────────────────────────────────────┐
│              用户浏览器（前端）                │
│  Vite + TypeScript + Tailwind CSS            │
│  登录/注册 → 审核界面 → 历史记录 → 管理面板    │
└──────────────────┬──────────────────────────┘
                   │ HTTP API
┌──────────────────▼──────────────────────────┐
│           Express 后端（端口 5000）            │
│                                              │
│  ┌─────────┐  ┌──────────┐  ┌────────────┐  │
│  │ 认证模块 │  │ 审核引擎  │  │ 管理接口   │  │
│  │ 登录/注册│  │ LLM+知识库│  │ 用户/知识库│  │
│  └────┬────┘  └────┬─────┘  └─────┬──────┘  │
│       │            │               │         │
└───────┼────────────┼───────────────┼─────────┘
        │            │               │
   ┌────▼────┐  ┌────▼─────┐  ┌─────▼──────┐
   │ Supabase│  │ Coze SDK │  │  Supabase  │
   │ 用户表  │  │ LLM      │  │  审核记录表 │
   │         │  │ Knowledge│  │            │
   │         │  │ Search   │  │            │
   └─────────┘  └──────────┘  └────────────┘
```

### 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端框架 | Vite + TypeScript | SPA 单页应用 |
| UI 样式 | Tailwind CSS v3 | 响应式布局 |
| 后端服务 | Express 4 | API 路由 + Vite 中间件 |
| 数据库 | Coze Supabase (PostgreSQL) | 用户表 + 审核记录表 |
| AI 模型 | doubao-seed-1-6-lite | 高性价比文件审核 + 错别字检测 |
| 知识库 | Coze Knowledge | 6 个独立数据集 |
| 联网搜索 | Coze Web Search | 知识库不足时补全 |

### 审核流程

```
用户上传文件 → 选择审核类型和模式
        │
        ▼
  ① 检索对应模块知识库（如：技术文件 → technical_document 数据集）
        │
        ▼ 知识库命中不足
  ② 联网搜索最新法规标准
        │
        ▼
  ③ 组装专用提示词（模块标准 + 错别字检测指令 + 知识库/联网资料 + 文件内容）
        │
        ▼
  ④ 调用 LLM 生成审核结果（含 annotatedContent 标注内容）
        │
        ▼
  ⑤ 存入数据库，返回对比标注视图
```

---

## 三、账号与权限

### 三种角色

| 角色 | 获取方式 | 权限 |
|------|---------|------|
| **管理员 (admin)** | 系统预设，默认账号 admin / 123456 | 全部功能 + 用户管理 + 知识库管理 |
| **普通用户 (user)** | 自行注册或管理员创建 | 文件审核 + 查看自己的历史记录 |
| **游客 (guest)** | 登录页点击"游客模式" | 仅文件审核，无历史记录 |

### 注册账号

1. 在登录页面点击「立即注册」
2. 填写用户名、密码、确认密码（显示名称可选）
3. 点击「注册」按钮，注册成功自动登录

### 修改密码

- 管理员：在「管理」Tab 中修改自己的密码
- 普通用户：登录后右上角点击头像 → 修改密码（管理员面板也可操作）

> **安全提示**：首次登录后请尽快修改默认密码。

---

## 四、功能使用

### 4.1 文件审核

1. **上传文件**：拖拽或点击上传区域，支持 PDF/Word/Excel/图片（单文件 ≤ 20MB）
2. **选择审核类型**：点击对应类型按钮（点击问号图标可查看各类型说明）
   - 👤 人员资质：身份证、特种作业证、安全考核证等
   - 🏢 企业资质：营业执照、安全生产许可证等
   - 📐 技术文件：施工方案、技术交底等
   - 🔒 安全检查：安全检查表、风险评估等
   - 📄 公文审核：通知、函件等（仅审核格式规范）
   - 🔍 全面审核：综合全部知识库审核
3. **选择审核模式**（点击问号图标可查看区别）
   - ⚡ 快速审核：重点检查关键问题 + 错别字，约 30 秒
   - 📋 详细审核：全面对照法规标准 + 错别字，约 1-2 分钟
4. **点击「开始审核」**

### 4.2 审核结果

审核完成后，右侧显示结果面板，包含以下 Tab：

| Tab | 说明 |
|-----|------|
| 📋 **对比标注**（默认） | 左右对比：左侧原始文件，右侧 AI 标注内容 |
| **问题** | 按严重程度列出所有问题（高/中/低） |
| **分析** | AI 的详细分析文字 |
| **建议** | 整改建议清单 |
| **来源** | 知识库/联网搜索的参考来源 |

### 标注说明

| 标注样式 | 含义 |
|---------|------|
| 🔴 **红色高亮** | 错别字，标注为"应为 XX" |
| ❌ **红色标注** | 审核问题/格式错误 |
| ⚠️ **黄色标注** | 需要注意的提醒事项 |

### 4.3 历史记录

- 管理员可查看所有审核记录
- 普通用户仅查看自己的记录
- 游客无历史记录功能
- 点击历史条目可查看审核详情
- 可删除不需要的记录

### 4.4 知识库管理（仅管理员）

1. 点击顶部「📚 知识库」Tab
2. **添加知识条目**：
   - 选择目标模块（如"公文审核"→ document_review 数据集）
   - 输入标题和内容（文本/链接）
   - 点击「添加」加入待导入列表
3. **按模块分类导入**：点击「按模块分类导入」，系统自动将知识条目导入到对应的数据集
4. **搜索测试**：可测试知识库检索和联网搜索效果

### 4.5 用户管理（仅管理员）

1. 点击顶部「👥 管理」Tab
2. **创建账号**：填写用户名/密码/显示名/角色
3. **编辑用户**：修改显示名、角色
4. **删除用户**：不能删除当前登录的管理员

---

## 五、知识库模块说明

| 数据集 ID | 模块名称 | 审核类型 | 审核内容 |
|-----------|---------|---------|---------|
| `personnel_qualification` | 人员资质 | personnel | 证件有效期、信息完整性、持证上岗合规性 |
| `enterprise_qualification` | 企业资质 | enterprise | 营业执照、安全生产许可证、资质等级匹配 |
| `technical_document` | 技术文件 | technical | 技术方案完整性、工艺合理性、审批流程合规 |
| `safety_inspection` | 安全检查 | safety | 安全检查表完整性、风险辨识、整改闭环 |
| `document_review` | 公文审核 | document | 仅格式规范（字体字号、页边距、签章、发文字号等） |
| `coze_doc_knowledge` | 通用法规 | comprehensive | 建设工程安全生产管理条例等综合法规 |

> **重要**：不同审核类型仅检索对应模块的知识库，确保审核标准的精准性。知识库不足时系统自动启用联网搜索补全。

---

## 六、关于费用

### 大模型（LLM）

当前使用的是 **doubao-seed-1-6-lite-251015** 模型，属于豆包系列中的高性价比模型，官方描述为"更高性价比，常见任务的最佳选择"。相比均衡型和旗舰型模型，该模型在保证审核质量的同时显著降低调用成本，适合 20-50 人规模的日常使用。

- **积分消耗**：每次审核会消耗 LLM 调用积分，详细审核消耗多于快速审核（因为输入/输出 token 更多）
- **知识库和联网搜索**：同样会消耗少量积分
- **建议**：日常筛查用快速审核，正式提交前用详细审核，可合理控制积分消耗
- **20-50 人使用**：完全可满足，服务器和数据库无压力，主要关注 LLM 积分余额即可

### 数据库（Supabase）

当前数据库总量约 61MB，其中审核记录表约 128KB（6 条记录），用户表约 80KB。

- **存储容量**：Supabase 免费额度提供 500MB 数据库存储空间，当前使用量远低于限制
- **数据估算**：每条审核记录约占 5-20KB（取决于文件内容长度和审核结果），可存储约 2-5 万条审核记录
- **超额处理**：如超过免费额度，需在 Coze 平台查看升级方案

> **提示**：可在 Coze 平台的「用量」页面查看当前积分和资源使用情况。

---

## 七、部署与访问

### 开发环境

```bash
pnpm install    # 安装依赖
pnpm dev        # 启动开发服务器（端口 5000，HMR 端口 6000）
pnpm build      # 构建生产版本
```

### 部署

项目部署后通过公网域名访问，无需额外配置。环境变量由平台自动注入。

### 后续更新

回到开发环境即可继续修改代码、增加功能，修改后重新部署即可生效。

---

## 八、常见问题

**Q：游客模式有什么限制？**
A：游客可以使用文件审核功能，但审核记录不会保存，无法查看历史记录，也看不到知识库和管理功能。

**Q：如何修改管理员默认密码？**
A：登录 admin 账号后，在「管理」Tab 中找到「修改我的密码」，输入原密码和新密码即可。

**Q：审核结果中的红色标注是什么意思？**
A：红色标注有两种：🔴 错别字标注（AI 检测到的错别字，会提示正确写法）和 ❌ 问题标注（审核发现的不合规项）。

**Q：知识库如何按模块分类？**
A：在知识库管理页面添加知识条目时，先选择目标模块，系统会自动将内容导入到对应的数据集。审核时只会检索对应模块的知识库。

**Q：联网搜索什么时候会启用？**
A：当知识库检索到的相关内容少于 2 条时，系统自动启用联网搜索补全最新法规标准。搜索结果会标注为"🌐 联网搜索"来源。

**Q：如何更换审核使用的 AI 模型？**
A：目前模型配置在后端代码 `server/routes/index.ts` 中，如需更换可修改 `model` 参数。可选模型包括 doubao-seed-2-0-pro（旗舰，最准但最贵）、doubao-seed-2-0-lite（均衡型）、doubao-seed-1-6-lite（当前使用，最高性价比）等。
